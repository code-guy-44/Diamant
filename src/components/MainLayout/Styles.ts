import { makeStyles } from '@mui/styles'

export const useStyles:any = makeStyles({
    mainContainer: {
        padding: '1em',
        textAlign: 'center'
    },
    card: {
        margin: '1em auto',
        padding: '1em',
        maxWidth: '12em'
    },
    hazardCards: {
        padding: '1em'
    }
})
